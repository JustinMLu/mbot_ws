"""LCM type definitions
This file automatically generated by lcm.
DO NOT MODIFY BY HAND!!!!
"""

try:
    import cStringIO.StringIO as BytesIO
except ImportError:
    from io import BytesIO
import struct

class slam_status_t(object):
    __slots__ = ["utime", "slam_mode", "map_path"]

    __typenames__ = ["int64_t", "int32_t", "string"]

    __dimensions__ = [None, None, None]

    def __init__(self):
        self.utime = 0
        self.slam_mode = 0
        self.map_path = ""

    def encode(self):
        buf = BytesIO()
        buf.write(slam_status_t._get_packed_fingerprint())
        self._encode_one(buf)
        return buf.getvalue()

    def _encode_one(self, buf):
        buf.write(struct.pack(">qi", self.utime, self.slam_mode))
        __map_path_encoded = self.map_path.encode('utf-8')
        buf.write(struct.pack('>I', len(__map_path_encoded)+1))
        buf.write(__map_path_encoded)
        buf.write(b"\0")

    def decode(data):
        if hasattr(data, 'read'):
            buf = data
        else:
            buf = BytesIO(data)
        if buf.read(8) != slam_status_t._get_packed_fingerprint():
            raise ValueError("Decode error")
        return slam_status_t._decode_one(buf)
    decode = staticmethod(decode)

    def _decode_one(buf):
        self = slam_status_t()
        self.utime, self.slam_mode = struct.unpack(">qi", buf.read(12))
        __map_path_len = struct.unpack('>I', buf.read(4))[0]
        self.map_path = buf.read(__map_path_len)[:-1].decode('utf-8', 'replace')
        return self
    _decode_one = staticmethod(_decode_one)

    def _get_hash_recursive(parents):
        if slam_status_t in parents: return 0
        tmphash = (0x522d41eb60d59eeb) & 0xffffffffffffffff
        tmphash  = (((tmphash<<1)&0xffffffffffffffff) + (tmphash>>63)) & 0xffffffffffffffff
        return tmphash
    _get_hash_recursive = staticmethod(_get_hash_recursive)
    _packed_fingerprint = None

    def _get_packed_fingerprint():
        if slam_status_t._packed_fingerprint is None:
            slam_status_t._packed_fingerprint = struct.pack(">Q", slam_status_t._get_hash_recursive([]))
        return slam_status_t._packed_fingerprint
    _get_packed_fingerprint = staticmethod(_get_packed_fingerprint)

    def get_hash(self):
        """Get the LCM hash of the struct"""
        return struct.unpack(">Q", slam_status_t._get_packed_fingerprint())[0]

